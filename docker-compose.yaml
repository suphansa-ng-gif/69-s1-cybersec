services:
  db:
    image: postgres:16-alpine
    container_name: 69-s1-db
    ports:
      - 54327:5432
    environment:
      - POSTGRES_PASSWORD=p@2004
      - POSTGRES_USER=suphansa
      - POSTGRES_DB=suphansa
    volumes:
      - ./data:/var/lib/postgresql/data
